# C4 — Arquitectura de Luces Errantes

El modelo **C4** describe la arquitectura en cuatro niveles de zoom progresivo. Cada nivel revela más detalle sin perder el contexto global.

| Nivel | Pregunta que responde |
|---|---|
| **L1 — Contexto** | ¿Quién usa el sistema y con qué servicios externos interactúa? |
| **L2 — Contenedores** | ¿Qué procesos o artefactos desplegables componen el sistema? |
| **L3 — Componentes** | ¿Qué piezas internas tiene cada contenedor? |
| **L4 — Código** | ¿Cómo se implementa cada componente? → ver código fuente |

---

## L1 — Contexto del Sistema

El sistema es un juego multijugador en tiempo real. Los jugadores acceden desde el navegador a través de Cloudflare, que gestiona el HTTPS y protege contra DDoS.

```mermaid
flowchart TB
    classDef person  fill:#08427B,color:#fff,stroke:#073B6F
    classDef system  fill:#1168BD,color:#fff,stroke:#0E5FAD
    classDef external fill:#6B6B6B,color:#fff,stroke:#555

    jugador["👤 Jugador
    ─────────────────
    Cualquier navegador moderno
    PC o móvil"]:::person

    cloudflare["☁️ Cloudflare
    ─────────────────
    CDN · Terminación HTTPS
    Protección DDoS
    WebSockets support requerido"]:::external

    sistema["🎮 Luces Errantes
    ─────────────────
    Juego multijugador en tiempo real
    Estilo Agar.io
    Svelte 5 + Go + WebSocket"]:::system

    jugador -- "Abre el juego vía HTTPS
    juega en tiempo real" --> cloudflare
    cloudflare -- "Reenvía tráfico HTTP
    + WebSocket upgrade" --> sistema
```

| Elemento | Tipo | Descripción |
|---|---|---|
| Jugador | Actor humano | Accede desde el navegador, interactúa solo con teclado y mouse |
| Cloudflare | Sistema externo | Gestiona HTTPS, CDN y protección DDoS. Requiere **WebSockets activado** en el dashboard |
| Luces Errantes | Sistema principal | Frontend + backend desplegados como contenedores Docker en un VPS |

---

## L2 — Contenedores

Cuatro contenedores cooperan para entregar el juego. El tráfico entra por Nginx Proxy Manager, que enruta hacia el frontend (nginx sirviendo la SPA), que a su vez hace proxy del WebSocket hacia el backend Go.

```mermaid
flowchart TB
    classDef person    fill:#08427B,color:#fff,stroke:#073B6F
    classDef external  fill:#6B6B6B,color:#fff,stroke:#555
    classDef container fill:#1168BD,color:#fff,stroke:#0E5FAD
    classDef infra     fill:#3A3A3A,color:#fff,stroke:#222

    jugador["👤 Jugador
    Navegador"]:::person

    cf["☁️ Cloudflare
    CDN / HTTPS Proxy"]:::external

    subgraph vps["🖥️  VPS — Linux"]
        npm["🔀 Nginx Proxy Manager
        ─────────────────────────
        Contenedor Docker externo
        Termina TLS · Enruta por dominio
        Habilitar: Websockets Support ✓
        Puertos host: 80 · 443"]:::infra

        subgraph stack["Stack del juego — docker-compose.yml"]
            fe["🌐 Frontend
            ──────────────────
            Imagen: nginx:alpine
            Sirve SPA Svelte + proxy WS
            Puerto host: 8060 → interno: 80"]:::container

            be["⚙️ Backend
            ──────────────────
            Imagen: alpine + binario Go
            Servidor WebSocket del juego
            Puerto interno: 8080 (no expuesto)"]:::container
        end
    end

    jugador   -- "HTTPS :443" --> cf
    cf        -- "HTTP (proxy)" --> npm
    npm       -- "HTTP + WS
    :443 → :8060 → :80" --> fe
    fe        -- "WebSocket proxy
    red interna Docker
    backend:8080" --> be
```

| Contenedor | Imagen base | Puerto | Responsabilidad |
|---|---|---|---|
| Nginx Proxy Manager | jc21/nginx-proxy-manager | 80 / 443 (host) | TLS, routing por dominio, WebSocket forwarding |
| Frontend | nginx:alpine | 8060→80 | Sirve el build de Svelte + proxy inverso de `/ws` al backend |
| Backend | alpine | 8080 (interno) | Motor del juego, conexiones WebSocket, broadcast de estado |

> **Red Docker interna**: frontend y backend comparten la red `game_test_default` creada por docker-compose. El frontend resuelve `backend` como hostname directamente sin exponer el puerto 8080 al host.

---

## L3 — Componentes del Frontend

El contenedor frontend tiene dos capas: **nginx** que sirve archivos estáticos y hace proxy del WebSocket, y la **SPA Svelte** que corre en el navegador del jugador.

```mermaid
flowchart TB
    classDef nginx   fill:#009639,color:#fff,stroke:#007A2E
    classDef svelte  fill:#FF3E00,color:#fff,stroke:#CC3200
    classDef canvas  fill:#7B4F00,color:#fff,stroke:#5C3A00
    classDef ext     fill:#6B6B6B,color:#fff,stroke:#555

    subgraph fe["Contenedor: Frontend (nginx:alpine)"]

        subgraph nginx_layer["nginx — Servidor HTTP :80"]
            static["📁 Static File Server
            location /
            Sirve index.html · CSS · JS
            SPA fallback: try_files → index.html"]:::nginx

            ws_proxy["🔀 WebSocket Proxy
            location /ws
            proxy_pass http://backend:8080/ws
            proxy_http_version 1.1
            Headers: Upgrade + Connection"]:::nginx
        end

        subgraph browser["Navegador del jugador (SPA compilada)"]

            subgraph screens["Pantallas"]
                login["🔐 Pantalla Login
                Canvas animado (partículas)
                Input nombre · Botón Jugar"]:::svelte

                game["🎮 Pantalla de Juego
                Canvas 2D pantalla completa
                Chat · Toasts · Ranking"]:::svelte
            end

            ws_client["📡 Cliente WebSocket
            ws(s)://host/ws
            ──────────────────────
            Envía → join · input · target · chat
            Recibe ← welcome · state · chat"]:::svelte

            subgraph render["Pipeline de Renderizado — drawGame()"]
                r_bg["⭐ Fondo oscuro
                Estrellas pulsantes
                Ondas sinusoidales"]:::canvas

                r_obs["🪨 Obstáculos
                Cache offscreen canvas
                Reconstruye solo al mover cámara"]:::canvas

                r_lights["💡 Luces
                Gradiente radial + core blanco
                Pulso sinusoidal"]:::canvas

                r_players["🔵 Jugadores
                Ordenados por tamaño
                Glow + highlight + anillo (yo)
                Interpolación de posición"]:::canvas

                r_ui["📊 UI Overlay
                Nombre · Tamaño (↖)
                Ranking top 10 (↗)
                Minimapa 140px (↘)
                Controles hint (↓, fade)"]:::canvas
            end

            interp["〰️ Interpolador
            updateInterpolation()
            50ms tick servidor → 60fps
            Suaviza movimiento entre ticks"]:::svelte
        end
    end

    browser_http["Petición HTTP"] -->|"GET /"| static
    browser_ws["WebSocket del browser"] -->|"GET /ws + Upgrade"| ws_proxy
    ws_proxy -.->|"proxy al backend"| be_ext["backend:8080"]:::ext
    static -->|"carga la SPA"| browser
    ws_client --> game
    login -->|"joinGame()"| ws_client
    game --> render
    interp --> r_players
```

| Componente | Archivo fuente | Función |
|---|---|---|
| Static File Server | `nginx.conf location /` | Sirve `dist/` con fallback `index.html` para rutas SPA |
| WebSocket Proxy | `nginx.conf location /ws` | Proxy al backend con headers `Upgrade` + `Connection: upgrade` |
| Pantalla Login | `App.svelte` | Canvas animado de fondo, formulario de nombre, inicia WS |
| Cliente WebSocket | `App.svelte joinGame()` | Abre `ws(s)://host/ws`, envía `join`, despacha mensajes recibidos |
| Pantalla de Juego | `App.svelte` | Canvas a pantalla completa + chat lateral + toasts de mención |
| Caché de Obstáculos | `App.svelte buildObstacleCache()` | Dibuja obstáculos en un canvas offscreen y lo reutiliza hasta que la cámara se aleja 100px |
| Interpolador | `App.svelte updateInterpolation()` | Interpola posiciones entre el tick anterior y el nuevo para 60fps fluidos |
| UI Overlay | `App.svelte drawGame()` | Dibuja info, ranking, minimapa y hint de controles sin transformación de cámara |

---

## L3 — Componentes del Backend

El contenedor backend es un único binario Go. Una **goroutine por cliente** gestiona I/O de WebSocket, y una **goroutine global única** ejecuta toda la lógica del juego — esto elimina la necesidad de locks para el estado compartido del juego.

```mermaid
flowchart TB
    classDef go        fill:#00ACD7,color:#fff,stroke:#0089AD
    classDef goroutine fill:#B8E4F0,color:#000,stroke:#4BAFC8
    classDef entity    fill:#E8F5E9,color:#000,stroke:#388E3C
    classDef channel   fill:#FFF3E0,color:#000,stroke:#E65100

    subgraph be["Contenedor: Backend (alpine + Go)"]

        subgraph main_go["main.go — Punto de entrada"]
            http["🌐 HTTP Server
            net/http :8080
            HandleFunc /ws"]:::go

            upgrader["⬆️ WebSocket Upgrader
            gorilla/websocket
            CheckOrigin: acepta todo"]:::go
        end

        subgraph hub_go["hub.go — Gestión de Clientes"]
            client["📦 Client{}
            conn *websocket.Conn
            send chan []byte
            game *Game · player *Player"]:::go

            read["📥 readPump()
            goroutine por cliente
            ──────────────────
            Lee frames WS del browser
            Decodifica JSON → ClientMessage
            Rutea a canales de Game"]:::goroutine

            write["📤 writePump()
            goroutine por cliente
            ──────────────────
            Escribe desde ← client.send
            Ping cada 54s (keep-alive)
            WriteDeadline: 10s"]:::goroutine
        end

        subgraph channels["Canales de comunicación"]
            ch_reg["register chan *Client"]:::channel
            ch_unreg["unregister chan *Client"]:::channel
            ch_join["join chan *JoinRequest"]:::channel
            ch_chat["chat chan *ChatRequest
            buffer: 64"]:::channel
        end

        subgraph game_go["game.go — Motor del Juego"]
            game_loop["🔄 Game Loop — Run()
            goroutine ÚNICA global
            select sobre todos los canales
            ──────────────────────────────
            ticker: 50ms (20 ticks/seg)
            spawnTimer: 2–5 seg aleatorio"]:::go

            gen_obs["🪨 generateObstacles()
            Al iniciar: 15–25 obstáculos
            Radio: 40–120px · sin solapamiento
            Se envían en welcome message"]:::go

            spawn["💡 spawnLight()
            Inicial: 15 luces
            Continuo: hasta máx. 40
            Evita spawnear en obstáculos"]:::go

            tick["⚡ tick() — Cada 50ms
            1. Update posición jugadores
            2. DeflectFromObstacle
            3. Colisión jugador↔luz
            4. Serializar estado JSON
            5. Calcular ranking top 10
            6. Broadcast a todos"]:::go
        end

        subgraph player_go["player.go — Entidad Jugador"]
            player["🟢 Player{}
            sync.RWMutex
            ──────────────────
            ID · Name · X · Y · Size · Color
            dx · dy · targetX · targetY"]:::entity

            movement["🕹️ Update(dt)
            Modo WASD: SetDirection(dx,dy)
            Modo click: SetTarget(x,y)
            velocidad = 200 / (1 + (size-15) × 0.008)
            Clamp dentro del mapa"]:::entity

            deflect["🔁 DeflectFromObstacle()
            Empuja al jugador fuera
            del radio del obstáculo"]:::entity
        end
    end

    http --> upgrader
    upgrader --> client
    client --> read
    client --> write

    read --> ch_reg
    read --> ch_unreg
    read --> ch_join
    read --> ch_chat
    read -->|"SetDirection / SetTarget"| player

    ch_reg --> game_loop
    ch_unreg --> game_loop
    ch_join --> game_loop
    ch_chat --> game_loop

    game_loop --> gen_obs
    game_loop --> spawn
    game_loop --> tick

    tick --> movement
    tick --> deflect
    tick -->|"← client.send"| write
```

| Componente | Archivo | Goroutines | Responsabilidad |
|---|---|---|---|
| HTTP Server | `main.go` | 1 (main) | Escucha en `:8080`, endpoint único `/ws` |
| WebSocket Upgrader | `main.go` | — | Convierte HTTP→WS, crea `Client{}`, arranca las 2 goroutines |
| Client | `hub.go` | 2 por cliente | Abstrae la conexión con canal de salida `send` |
| readPump | `hub.go` | 1 por cliente | Lee frames, decodifica JSON, envía a canales del Game; deadline 60s reset por pong |
| writePump | `hub.go` | 1 por cliente | Escribe desde `send`, envía ping cada 54s; deadline 10s por escritura |
| Game Loop | `game.go` | 1 global | Único modificador del estado del juego — sin data races en maps/players |
| generateObstacles | `game.go` | — | Genera 15–25 obstáculos al iniciar, sin solapamiento, radio 40–120px |
| spawnLight | `game.go` | — | Crea luces aleatorias fuera de obstáculos; inicial 15, máx. 40 |
| tick | `game.go` | — | Paso de simulación: move → deflect → collide → rank → broadcast |
| Player | `player.go` | — | Entidad con `sync.RWMutex` porque `readPump` la accede desde otra goroutine |

---

## Flujo WebSocket — De punta a punta

```mermaid
sequenceDiagram
    actor J as 👤 Jugador
    participant CF as ☁️ Cloudflare
    participant NPM as 🔀 Nginx Proxy Manager
    participant FE as 🌐 Frontend (nginx)
    participant BE as ⚙️ Backend (Go)

    J->>CF: HTTPS GET https://dominio.com
    CF->>NPM: HTTP GET /
    NPM->>FE: HTTP GET / (:443 → :8060 → :80)
    FE-->>J: index.html + CSS + JS (Svelte SPA)

    Note over J: Escribe nombre → presiona "Jugar"

    J->>CF: wss://dominio.com/ws (Upgrade)
    CF->>NPM: WS Upgrade [WebSockets: ON ✓]
    NPM->>FE: WS Upgrade :8060 → :80
    FE->>BE: proxy_pass backend:8080/ws
    BE-->>J: 101 Switching Protocols ✓

    J->>BE: { type:"join", name:"Ana" }
    BE-->>J: { type:"welcome", id:"p1", mapW:4000, mapH:4000, obstacles:[...] }

    Note over J: Carga mapa · genera estrellas · inicia gameLoop()

    loop Cada 50ms (20 ticks/seg)
        J->>BE: { type:"input", dx:1, dy:0 }
        BE->>BE: tick(): move → deflect → collide → rank
        BE-->>J: { type:"state", players:[...], lights:[...], top:[...] }
        Note over J: Interpolación 50ms → 60fps en canvas
    end

    opt Click en canvas
        J->>BE: { type:"target", x:2000, y:1500 }
        Note over BE: SetTarget() → click-to-move
    end

    opt Mensaje de chat
        J->>BE: { type:"chat", text:"hola @Bob" }
        BE-->>J: { type:"chat", sender:"Ana", text:"hola @Bob", timestamp:... }
        Note over J: Toast si te mencionan con @nombre
    end

    loop Cada 54s
        BE-->>J: Ping frame
        J-->>BE: Pong frame
        Note over BE: Resetea ReadDeadline (60s)
    end
```

---

## Protocolo de Mensajes

### Cliente → Servidor

| Tipo | Campos | Cuándo |
|---|---|---|
| `join` | `name` (max 20 chars) | Una vez al conectar |
| `input` | `dx`, `dy` (−1, 0, 1) | Cada keydown / keyup |
| `target` | `x`, `y` (coord. mundo) | Cada click en el canvas |
| `chat` | `text` (max 200 chars) | Al enviar mensaje |

### Servidor → Cliente

| Tipo | Campos | Frecuencia |
|---|---|---|
| `welcome` | `id`, `mapW`, `mapH`, `obstacles[]` | 1 vez tras `join` |
| `state` | `players[]`, `lights[]`, `top[]` | 20 veces/seg |
| `chat` | `sender`, `text`, `timestamp` | Al recibir mensaje de cualquier jugador |

---

## Despliegue

```mermaid
flowchart LR
    classDef script  fill:#E76F51,color:#fff,stroke:#C45C40
    classDef file    fill:#2A9D8F,color:#fff,stroke:#1E7E74
    classDef image   fill:#457B9D,color:#fff,stroke:#335F7A
    classDef running fill:#1D3557,color:#fff,stroke:#152740
    classDef net     fill:#264653,color:#fff,stroke:#1A3040

    deploy["▶ ./deploy.sh
    ─────────────────
    1. Valida Docker instalado y corriendo
    2. Baja stack anterior si existe
    3. docker compose up --build -d
    4. Espera healthcheck del backend
    5. Valida HTTP :8060 → 200
    6. Valida WS /ws → 101
    7. Muestra logs en caso de error"]:::script

    subgraph build["Archivos de build"]
        dc["docker-compose.yml
        Declara servicios
        Healthcheck backend
        Red interna
        Puerto 8060:80"]:::file

        df_fe["zen-garden/Dockerfile
        Stage 1: node:20-alpine
          npm ci + vite build
        Stage 2: nginx:alpine
          copia dist/
          copia nginx.conf"]:::file

        df_be["zen-garden-server/Dockerfile
        Stage 1: golang:alpine
          go mod download
          go build
        Stage 2: alpine
          copia binario"]:::file
    end

    subgraph images["Imágenes construidas"]
        img_fe["game_test-frontend
        nginx:alpine
        ~25 MB"]:::image

        img_be["game_test-backend
        alpine + binario Go
        ~15 MB"]:::image
    end

    subgraph running["Contenedores en ejecución"]
        ct_fe["game_test-frontend-1
        :8060 → :80
        restart: unless-stopped"]:::running

        ct_be["game_test-backend-1
        :8080 interno
        restart: unless-stopped
        healthcheck: wget /ws"]:::running

        net_d["red: game_test_default
        DNS interno: 'backend'
        resuelve al contenedor Go"]:::net
    end

    deploy --> dc
    dc --> df_fe & df_be
    df_fe --> img_fe --> ct_fe
    df_be --> img_be --> ct_be
    ct_fe --- net_d
    ct_be --- net_d
```

| Archivo | Ubicación | Función |
|---|---|---|
| `deploy.sh` | raíz | Orquesta todo: checks → down → build → up → validate |
| `docker-compose.yml` | raíz | Declara los dos servicios, red interna y healthcheck |
| `Dockerfile` | `zen-garden/` | Build multi-stage: Svelte → nginx con SPA |
| `Dockerfile` | `zen-garden-server/` | Build multi-stage: compilación Go → binario mínimo |
| `nginx.conf` | `zen-garden/` | Sirve SPA + proxy WebSocket al backend |
| `.dockerignore` | `zen-garden/` | Excluye `node_modules/` y `dist/` del contexto de build |

---

## Resumen de la cadena completa

```
Jugador (browser)
    │
    │  wss://dominio.com/ws  (HTTPS/WSS puerto 443)
    ▼
Cloudflare
    │  WebSockets: ON requerido en Dashboard → Network
    │  HTTP (proxy inverso hacia el VPS)
    ▼
Nginx Proxy Manager  [contenedor Docker, VPS]
    │  Websockets Support: ON requerido en Proxy Host
    │  Enruta dominio → localhost:8060
    ▼
Frontend  [contenedor Docker, imagen nginx:alpine]
    │  Puerto 8060 (host) → 80 (contenedor)
    ├── GET /     → sirve build de Svelte (index.html + assets)
    └── GET /ws   → proxy_pass http://backend:8080/ws
                              │
                              │  WebSocket puro, red interna Docker
                              ▼
Backend  [contenedor Docker, binario Go en alpine]
    │  Puerto 8080 (solo red interna, no expuesto al host)
    │
    ├── 1 goroutine main         → HTTP server, acepta conexiones
    ├── 2 goroutines por cliente → readPump + writePump
    └── 1 goroutine global       → Game Loop (20 ticks/seg)
                                      ├── Mueve jugadores
                                      ├── Deflecta de obstáculos
                                      ├── Colisiones jugador ↔ luz
                                      ├── Ranking top 10
                                      └── Broadcast estado JSON
```
