services:
  backend:
    build: .
    restart: unless-stopped

  frontend:
    build:
      context: ../zen-garden
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
